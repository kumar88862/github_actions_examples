name: input examples

on:
  workflow_dispatch:
    input:
      app_name: 
        description: "Application Name"
        required: true
        type: string
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: example
        run: ls 
