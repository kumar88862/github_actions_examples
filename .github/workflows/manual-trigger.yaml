name: Manual Workflow Example

on:
  workflow_dispatch:

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "This workflow was triggered manually!"